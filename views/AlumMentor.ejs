<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <style>
        * {
            box-sizing: border-box;
        }

        body {
            display: flex;
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            height: 100vh;
            background-color: #f0f2f5;
        }

        /* Sidebar Styles */
        .sidebar {
            width: 250px;
            background-color: #343a40;
            color: #ffffff;
            padding: 20px;
            height: 100%;
        }

        .sidebar h2 {
            margin-bottom: 20px;
        }

        .sidebar ul {
            list-style-type: none;
            padding: 0;
        }

        .sidebar ul li {
            margin: 15px 0;
        }

        .sidebar ul li a {
            color: #ffffff;
            text-decoration: none;
            font-weight: 500;
        }

        .sidebar ul li a:hover {
            text-decoration: underline;
        }

        /* Main Content Styles */
        .main-content {
            flex: 1;
            padding: 20px;
        }

        /* Student Card Styles */
        .student-card {
            background-color: #ffffff;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            padding: 16px;
            margin-bottom: 20px;
            transition: transform 0.2s;
            width: 250px; /* Makes the card squarish */
            height: 250px; /* Makes the card squarish */
            display: flex;
            flex-direction: column;
            justify-content: space-between;
        }

        .student-card:hover {
            transform: scale(1.02);
        }

        .student-name {
            font-size: 1.25rem;
            font-weight: 600;
            color: #333;
            margin: 0;
        }

        .student-domain {
            font-size: 1rem;
            color: #666;
            margin: 0;
        }

        .radio-group {
            display: flex;
            justify-content: space-between;
            margin-top: auto;
        }

        .radio-group label {
            display: flex;
            align-items: center;
            font-size: 1rem;
            color: #333;
        }

        .radio-group input {
            margin-right: 5px;
        }
    </style>
</head>

<body>
    <div class="sidebar">
        <h2>Dashboard</h2>
        <ul>
            <li><a href="jobs.html">Job Listing</a></li>
            <li><a href="events.html">Events</a></li>
            <li><a href="job-applicants.html">Job Applicants</a></li>
            <li><a href="settings.html">Settings</a></li>
        </ul>
    </div>

    <div class="main-content">
        <h1>Student Applicants</h1>

        <div class="student-card">
            <h3 class="student-name">John Doe</h3>
            <p class="student-domain">Domain: Software Development</p>
            <form class="radio-group">
                <label>
                    <input type="radio" name="response-JohnDoe" value="accept"> Accept
                </label>
                <label>
                    <input type="radio" name="response-JohnDoe" value="reject"> Reject
                </label>
            </form>
        </div>
    </div>
</body>

</html>
